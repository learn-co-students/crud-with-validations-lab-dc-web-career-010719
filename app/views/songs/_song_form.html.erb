<%= form_for @song do |s| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@song.errors.count, "error")%> prohibited this post from being saved:
      </h2>
      <ul>
        <% @song.errors.full_messages.each do |m| %>
          <li><%= m %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= s.label :title %>
  <%= s.text_field :title %><br><br>
  <%= s.label :released %>
  <%= s.check_box :released %><br><br>
  <%= s.label :release_year %>
  <%= s.number_field :release_year %><br><br>
  <%= s.label :artist_name %>
  <%= s.text_field :artist_name %><br><br>
  <%= s.label :genre %>
  <%= s.text_field :genre %><br><br>
  <%= s.submit %>
<% end %>
